## MODIFIED Requirements

### Requirement: 申請状態と本人による取消
残業・休日出勤申請は`pending`、`returned`、`approved`、`rejected`、`cancelled`の状態、競合検知用version、不変の連番改訂を持ち、`pending`から`returned`または一つの最終状態へ、`returned`から新しい改訂の`pending`または`cancelled`へだけ遷移できなければならない（MUST）。従業員は本人作成または管理者が代理作成した自分の申請内容、代理表示、各改訂、審査結果を閲覧し、締め済みでない月に属する自分の`pending`または`returned`申請だけを取り消せなければならない（MUST）。再申請時は現在の勤務カレンダー、勤務ルール、残業申請ポリシー、重複、締め状態を新規提出と同様に再検査しなければならない（MUST）。

#### Scenario: 審査待ち申請を取り消す
- **WHEN** 従業員が自分の最新バージョンの`pending`申請を取り消す
- **THEN** システムは申請を`cancelled`に変更し、勤怠実績を変更せず審査一覧から除外する

#### Scenario: 差し戻された残業申請を再申請する
- **WHEN** 従業員が`returned`申請の予定時刻、休憩、理由を修正し、現在のポリシーと勤務予定に対する検証を満たして再申請する
- **THEN** システムは過去改訂を保持して新しい改訂を追記し、申請を`pending`へ戻して現在の承認担当者へ割り当てる

#### Scenario: 再申請時にポリシーへ違反する
- **WHEN** 差し戻し後のポリシー変更または時刻経過により、再申請が入力単位、事前申請要件、重複、締め状態のいずれかを満たさない
- **THEN** システムは申請を`returned`のまま保持し、現在の阻害理由を表示する

#### Scenario: 審査済み申請を取り消す
- **WHEN** 従業員が`approved`または`rejected`の申請を取り消そうとする
- **THEN** システムは状態を変更せず、管理者へ連絡するよう表示する

#### Scenario: 古い画面から申請を更新する
- **WHEN** 利用者が現在と一致しない期待versionまたは改訂番号で取消、再申請、差し戻し、審査を実行する
- **THEN** システムは操作を拒否し、最新状態、担当者、申請内容を再読込するよう表示する

### Requirement: 原子的な単段階審査
所有者または労務管理者は同一組織の審査待ち申請を、承認担当者は自分へ現在割り当てられた審査待ち申請だけを対象に、申請理由、代理作成有無、各改訂、勤務予定、カレンダー根拠、予定時間、既存申請、現在の打刻を確認し、対象従業員本人および代理作成者以外が承認、理由付き却下、または理由付き差し戻しできなければならない（MUST）。審査時は対象月、申請、承認ケースを排他的に処理し、割当、期待version、締め状態、在籍、ポリシー、重複を再検査して、申請・ケース状態、審査情報、通知、監査記録を単一トランザクションで保存しなければならない（MUST）。

#### Scenario: 割当承認担当者が競合しない申請を承認する
- **WHEN** 対象従業員および代理作成者とは異なる承認担当者が、自分へ割り当てられた未締め月の最新申請を承認する
- **THEN** システムは申請を`approved`にし、審査者・日時、使用した改訂、申請時点の勤務予定、経路を保持して通知と監査記録を作成する

#### Scenario: 理由付きで却下する
- **WHEN** 審査者が具体的な却下理由を入力して申請を却下する
- **THEN** システムは申請を`rejected`にし、申請者へ理由を含む通知を作成する

#### Scenario: 修正が必要な申請を差し戻す
- **WHEN** 審査者が具体的な差し戻し理由を入力して最新の`pending`申請を差し戻す
- **THEN** システムは申請を`returned`にし、勤怠実績を変更せず、対象従業員へ理由と再申請導線を通知する

#### Scenario: 対象従業員または代理作成者が自己承認する
- **WHEN** 審査actorが対象従業員に紐付く利用者または申請の代理作成者と一致する
- **THEN** システムは審査を拒否し、申請、通知、監査記録を変更しない

#### Scenario: 承認担当者が未割当申請を審査する
- **WHEN** `approver`が自分へ割り当てられていない残業・休日出勤申請を審査しようとする
- **THEN** システムは申請内容を返さず、申請、通知、監査記録を変更しない

#### Scenario: 審査中に対象月が締まる
- **WHEN** 同じ対象月の締め処理と申請承認が競合する
- **THEN** システムは月単位で直列化し、締め後の承認を拒否するか、承認後の状態を締め前検査へ反映する

### Requirement: 組織境界と役割別アクセス
システムは、従業員には本人作成または代理作成された自分の申請・差異だけを、承認担当者には自分へ現在割り当てられた申請とその審査に必要な情報だけを、所有者と労務管理者には同一組織の申請・差異だけを表示しなければならない（MUST）。APIは組織、対象従業員、担当範囲を認証済み利用者と承認ケースから導出し、他組織ID、他者申請ID、未割当申請ID、無効利用者、改ざんされた通知先を受け付けてはならない（MUST）。

#### Scenario: 従業員が他者申請を開く
- **WHEN** 従業員がURLまたはAPIで同一組織の別従業員の申請IDを指定する
- **THEN** システムは申請の存在や内容を露出せずアクセスを拒否する

#### Scenario: 承認担当者が割当申請を開く
- **WHEN** `approver`が自分へ現在割り当てられた残業・休日出勤申請を開く
- **THEN** システムは対象申請、勤務予定、現在打刻、実績差異、経路根拠、許可された審査操作を表示する

#### Scenario: 承認担当者が未割当申請を開く
- **WHEN** `approver`がURLまたはAPIで別担当者または管理者共通プールの申請IDを指定する
- **THEN** システムは申請、従業員、勤務予定、差異の存在を露出せずアクセスを拒否する

#### Scenario: 管理者が他組織IDを送る
- **WHEN** 管理者が別組織の従業員、申請、ポリシー、承認ケース識別子を送信する
- **THEN** システムは認証済み組織の範囲外を参照または変更しない

## ADDED Requirements

### Requirement: 管理者による代理残業・休日出勤申請
所有者または労務管理者は、締め済みでない同一組織の従業員について、対象勤務日、残業または休日出勤、予定開始・終了、予定休憩、申請理由、具体的な代理理由を指定し、本人に代わって申請を作成できなければならない（MUST）。システムは実際の作成者と対象従業員を分け、本人申請と同じ勤務カレンダー、勤務ルール、ポリシー、入力単位、事前申請要件、重複、締め状態を検証しなければならない（MUST）。

#### Scenario: 管理者が残業を代理申請する
- **WHEN** 労務管理者が従業員、未締め勤務日、有効な予定時刻・休憩、申請理由、代理理由を指定して提出する
- **THEN** システムは対象従業員、代理作成者、代理理由、予定分数、経路を持つ`pending`申請を作成し、勤怠実績へ予定分数を加算しない

#### Scenario: 事前申請要件を代理権限で回避する
- **WHEN** 事前申請を必須とするポリシーの予定開始後に管理者が代理申請する
- **THEN** システムは申請を作成せず、事後申請を許可するポリシーまたは別の勤怠処理が必要であることを表示する

#### Scenario: 本人が代理申請を確認する
- **WHEN** 対象従業員が自分について管理者が代理作成した残業・休日出勤申請を開く
- **THEN** システムは代理作成であること、作成者、代理理由、予定、現在状態を表示する
