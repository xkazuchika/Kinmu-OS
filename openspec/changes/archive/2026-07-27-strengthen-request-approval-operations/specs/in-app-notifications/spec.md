## ADDED Requirements

### Requirement: 全申請種別と承認運用イベントのアプリ内通知
システムは、休暇、勤怠修正、残業・休日出勤の提出・再申請時に現在の実担当者へ、差し戻し・承認・却下時に対象従業員へ、取消時に現在の実担当者へ、再割当時に新旧担当者へアプリ内通知を作成しなければならない（MUST）。代理作成された申請の差し戻し・承認・却下では対象従業員と、現在も権限を持つ代理作成者へ通知しなければならない（MUST）。通知作成は申請、承認ケース、ドメイン結果、監査記録と同じトランザクションで完了し、任意URLまたは権限外の申請内容を保存してはならない。

#### Scenario: 休暇申請を提出する
- **WHEN** 休暇申請と承認ケースが`pending`として保存され、実担当者が決定する
- **THEN** システムは実担当者へ申請種別、対象日、期限、ケース参照を持つ審査待ち通知を作成する

#### Scenario: 勤怠修正を差し戻す
- **WHEN** 審査者が勤怠修正申請を具体的な理由付きで`returned`にする
- **THEN** システムは対象従業員へ差し戻し結果、許可された理由要約、再申請導線を持つ通知を作成する

#### Scenario: 差し戻された申請を再申請する
- **WHEN** 対象従業員または許可された代理作成者が新しい改訂を`pending`として提出する
- **THEN** システムは現在の実担当者へ申請種別、新しい版、再申請であることを示す通知を作成する

#### Scenario: 審査待ち申請を再割当する
- **WHEN** 所有者または労務管理者が`pending`ケースを別の有効な承認担当者へ再割当する
- **THEN** システムは新担当者へ審査依頼、旧担当者へ担当解除の最小通知を作成し、両者の現在権限に応じた導線を生成する

#### Scenario: 代理申請を承認する
- **WHEN** 代理作成された申請が承認される
- **THEN** システムは対象従業員と現在も権限を持つ代理作成者へ、代理作成であることと審査結果を示す通知を作成する

#### Scenario: 保存が途中で失敗する
- **WHEN** 申請改訂、承認ケース、ドメイン結果、監査記録、通知のいずれかの保存が失敗する
- **THEN** システムはトランザクション全体を取り消し、状態変更を伴わない部分的な通知を残さない

### Requirement: 現在の割当権限を再検査する通知導線
承認運用通知は申請種別と承認ケースIDだけから許可検査後のアプリ内導線を生成し、受信後の再割当、役割変更、無効化、申請状態変更により閲覧できなくなった情報を露出してはならない（MUST）。通知一覧の件名と要約も、受信者が現在閲覧できる最小限の履歴に制限しなければならない。

#### Scenario: 再割当された旧担当者が通知を開く
- **WHEN** 旧担当者が担当解除後に過去の審査待ち通知を開く
- **THEN** システムは担当解除の履歴だけを表示し、申請者、理由、差分、審査操作へ遷移させない

#### Scenario: 対象従業員が結果通知を開く
- **WHEN** 対象従業員が自分の申請に対する差し戻し、承認、または却下通知を開く
- **THEN** システムは本人が閲覧できる最新改訂と審査結果へ遷移し、承認担当者専用情報を表示しない
