# Tasks

## 1. 判定と認可

- [x] 1.1 要対応の型・時計・4種類の判定を実装し、日付境界、期限同時刻、期限なし、差し戻し当日、休日・休暇・欠勤・未有効カレンダー・締め済みの単体テストを通す
- [x] 1.2 既存勤怠投影を再利用する読取サービスを実装し、古い未締め月、在籍履歴、打刻のみの過去月、複数日休暇、関連する審査待ち申請のDB統合テストで一覧の整合を確認する
- [x] 1.3 現在の閲覧・審査・再申請・再割当規則と担当表示を共通化し、本人・approver・管理者・代理作成者・自己審査・担当無効化・引継ぎ終了・他組織の認可テストを通す
- [x] 1.4 要対応読取API、種類別件数、安定ソートと50件ページングを実装し、フィルター検証、対象情報の漏えい防止、件数一致、キャッシュ禁止をAPIテストで確認する

## 2. 要対応画面と解消導線

- [x] 2.1 要対応画面、ホーム要約、役割別ナビゲーション、画面カタログを追加し、0件・検索0件・読込・エラーのUIテストと既存画面カタログ検証を通す
- [x] 2.2 勤怠・修正・休暇・欠勤・申請審査の既存画面へ対象日と安全な戻り先を引き継ぎ、修正承認・再申請・取消後に元の条件へ戻ると一覧と件数から解消分が外れるE2Eを通す
- [x] 2.3 審査待ちの関連申請、本人の対応待ち、再割当要否を表示し、重複申請や自己審査を促さないことを役割別ブラウザテストで確認する
- [x] 2.4 320pxとデスクトップで対象・理由・担当・操作を確認し、横スクロールなし、キーボード操作、状態の読み上げ、既存打刻・月次進捗の維持をブラウザで検証する

## 3. 日次通知とworker

- [x] 3.1 通知種別と必要な索引の加算マイグレーションを追加し、v0.8相当DBの移行・再実行と既存通知の保持を移行テストで確認する
- [x] 3.2 共通判定に基づく日次ダイジェスト生成を実装し、9時境界、タイムゾーン・夏時間、差し戻し翌日、当日の新規対象、兼務、無効利用者、未割当・要再割当の受信者テストを通す
- [x] 3.3 DBロック・日次eventKey・トランザクションを実装し、並行実行、再試行、組織単位の失敗とロールバック、既読維持、停止後の当日分だけの再開をDB統合テストで確認する
- [x] 3.4 リマインダーの最小本文・通知日・現在権限による対象導線を実装し、全件解消後、再割当後、降格後、他者通知ID指定、既読後の未解消維持を統合テストで確認する
- [x] 3.5 一回実行と15分間隔の常駐worker、終了処理、状態ファイル・ヘルスチェック・安全なログを追加し、時計を制御したテストで失敗・再試行・30分超の異常判定・非ゼロ終了を確認する

## 4. 配布・更新・運用

- [x] 4.1 非rootのworkerイメージ、Composeサービス、開発・一回実行用package scriptsを追加し、移行成功後の起動とログインなしの通知生成を隔離Composeで確認する
- [x] 4.2 更新スクリプトをappとworkerに対応させ、初導入・通常更新・ビルド失敗・移行失敗で旧appとworker停止状態が設計どおりになる運用スクリプトテストを通す
- [x] 4.3 隔離Composeでworker再起動、DB停止・復旧、正常更新、バックアップと空DB復元を実測し、通知重複がなく勤怠・申請・締め・給与runが保持されることを記録する
- [x] 4.4 要対応・通知の利用ガイドとREADMEの起動・監視・停止・一回実行・復元・切り戻し手順を更新し、記事カタログ・リンク検証と文書化したコマンドの動作確認を通す

## 5. 総合検証

- [x] 5.1 100名×3か月・300ケースで一覧とホーム集計が各5秒未満、workerが1組織30秒未満になる性能テストを通し、長年未締めの別fixtureも実測して結果と条件を記録する
- [x] 5.2 型・Lint・整形・単体/UI・専用DB統合/移行・本番build・主要E2E・依存監査を実行し、新機能と既存承認・締め・給与連携の回帰がないことを確認する
- [x] 5.3 仕様シナリオとテストの対応表・検証結果・未検証範囲を記録し、OpenSpec strict validationとgit diff --checkを通す

## 6. 追加承認された依存更新

- [x] 6.1 2026-09-23の依存監査で見つかった脆弱性を修正版へ更新し、監査・型・Lint・テスト・build・E2Eを再検証する（ユーザー承認済み）
